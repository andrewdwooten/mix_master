<h1>New Song</h1>
<h3>Add a New Song by: <%= @artist.name %></h3>
<%= render partial: "shared/errors", locals: { target: @song } %>
<%= form_for([@artist, @song]) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.submit %>
<% end %>
